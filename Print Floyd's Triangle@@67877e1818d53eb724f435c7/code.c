#include <stdio.h>
int main()
{
    int i,j,a;
    scanf("%d", &a);
    for(i=i;i<=a;i++)
    {
        for(j=i;j<=a;j++)
        {
            printf("%d ", j);
        }
        printf("\n");
    }
    return 0;
}